<Window x:Class="AdministradorCervezas.Views.PrincipalView"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdministradorCervezas.Views"
        mc:Ignorable="d"
        Title="Administrador Cervezas" Height="700" Width="1024" 
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Window.Background>
        <ImageBrush ImageSource="/AdministradorCervezas;component/Resources/background.jpg"></ImageBrush>
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="55"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1.7*"/>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>

        <!--Titulo Ajustes y Ayuda-->

        <Grid Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="0.5*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="10"/>
            </Grid.RowDefinitions>
            <Button Margin="705,0,10,2.6">Ayuda</Button>
            <Button Margin="2,0,0,1.6"  Grid.Column="1">Salir</Button>
            <TextBlock Style="{StaticResource Titulo}" 
                       Grid.Column="0" 
                       Grid.ColumnSpan="2">Sistema Administrador Brewery</TextBlock>

        </Grid>

        <!--Menu Botones-->

        <Grid Grid.Row="2" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Button Style="{StaticResource Secciones}" x:Name="CargaCervezas" Grid.Row="0">Cervezas</Button>
            <Button Style="{StaticResource Secciones}" x:Name="CargaMarcas" Grid.Row="1">Marcas</Button>
            <Button Style="{StaticResource Secciones}" x:Name="CargaModelos" Grid.Row="2">Modelos</Button>
            <Button Style="{StaticResource Secciones}" x:Name="CargaClientes" Grid.Row="3">Clientes</Button>
            <Button Style="{StaticResource Secciones}" x:Name="CargaOrdenes" Grid.Row="4">Ordenes</Button>
        </Grid>

        <!--Contenido -->

        <Grid Grid.Column="2"
              Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
            </Grid.RowDefinitions>
            <ContentControl Grid.Row="0"
                            Grid.Column="1"
                            x:Name="ActiveItem">
            </ContentControl>

        </Grid>
    </Grid>
</Window>
